<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.litterae.familyorganizer">

    <application
        android:name=".application.MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".implementation.notifications.NotificationsActivity"></activity>
        <activity android:name=".implementation.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".implementation.groupchat.GroupChatActivity" />
        <activity android:name=".implementation.singlechat.ChatActivity" />
        <activity android:name=".implementation.family.FamilyActivity" />
        <activity android:name=".implementation.notes.NotesActivity" />
        <activity android:name=".implementation.shopping.ShoppingActivity" />
        <activity android:name=".implementation.planner.PlannerActivity" />
    </application>

</manifest>